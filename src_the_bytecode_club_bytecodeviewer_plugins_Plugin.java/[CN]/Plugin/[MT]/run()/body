{
  BytecodeViewer.viewer.setIcon(true);
  try {
    if (!BytecodeViewer.getLoadedClasses().isEmpty())     execute(BytecodeViewer.getLoadedClasses());
 else     System.out.println("Plugin not ran, put some classes in first.");
  }
 catch (  Exception e) {
    e.printStackTrace();
  }
 finally {
    finished=true;
    BytecodeViewer.viewer.setIcon(false);
  }
}
