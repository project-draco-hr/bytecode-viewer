{
  return new InsnAnalyzer(){
    @Override public boolean accept(    final AbstractInsnNode node){
      if (node instanceof IntInsnNode)       return node.getOpcode() == opcode && ((IntInsnNode)node).operand == value;
 else       return false;
    }
  }
;
}
