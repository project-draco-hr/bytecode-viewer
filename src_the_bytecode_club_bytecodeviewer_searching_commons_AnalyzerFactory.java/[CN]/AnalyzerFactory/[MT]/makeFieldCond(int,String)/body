{
  return new InsnAnalyzer(){
    @Override public boolean accept(    final AbstractInsnNode node){
      if (node instanceof FieldInsnNode)       return node.getOpcode() == opcode && ((FieldInsnNode)node).desc.equals(type);
 else       return false;
    }
  }
;
}
