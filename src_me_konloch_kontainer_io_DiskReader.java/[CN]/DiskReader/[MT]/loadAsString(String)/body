{
  String s="";
  BufferedReader reader=new BufferedReader(new FileReader(new File(fileName)));
  String add;
  while ((add=reader.readLine()) != null)   s+=add + System.getProperty("line.separator");
  reader.close();
  return s;
}
