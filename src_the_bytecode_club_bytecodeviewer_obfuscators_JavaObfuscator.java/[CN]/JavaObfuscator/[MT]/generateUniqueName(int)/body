{
  boolean found=false;
  String name="";
  while (!found) {
    String nameTry=randomString(1) + randomStringNum(length - 1);
    if (!names.contains(nameTry)) {
      names.add(nameTry);
      name=nameTry;
      found=true;
    }
  }
  return name;
}
